source "https://rubygems.org"

# Use the github-pages gem so local builds match GitHub Pages' build environment.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
